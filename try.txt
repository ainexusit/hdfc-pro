try:
risky code

except:
handling code

finally:
clean up code


try:

conn =  pymysql.connect() # open 

data transfer issue

except:


finally:
    fp.close()
    conn.close()
    logout()

try:

risky code

except:

handling/alternate code

else:
additional code


finally:

clean up code



1.if-else  --  if condiation failed/false then else block execute
2.for-else --  for loop break else part will not execute
3.try-else -- if no exception in try then else will execute

try:
    age = int(input("Enter the age:"))
    if age>18:
        print("process")
    else:
        raise Exception("Age should be greater than 18")
except Exception as e:
    print(e)


def function():
   try:



try:
    username = input("Enter the username:")
    if username=='admin':
        password = input("Enter the password:")
        if password=='admin@123':
            print("login")
        else:
            raise Exception("password wrong")
except Exception as e:
    try:
        password = input("Enter the password:")
        if password == 'admin@123':
            print("login")
        else:
            raise Exception("password wrong")
    except Exception as e:
        try:
            password = input("Enter the password:")
            if password == 'admin@123':
                print("login")
            else:
                raise Exception("password wrong")
        except Exception as e:
            print("Account Blocked")
finally:
    print("logout")

logging:
========


1.CRITICAL - 50
2.ERROR    - 40
3.WARNING  - 30
4.INFO     - 20
5.DEBUG    -10

default:
log message displayed in :console

level : 30 and above

message format:
CRITICAL:root:critical message

level : logger : message


basicConfig(filename='server.log',
		
import logging

logging.basicConfig(filename='server.log',level=10,
                    format='%(asctime)s:%(levelname)s:%(message)s')

logging.critical("critical message")
logging.error("error message")
logging.warning("warning message")
logging.info("info message")
logging.debug("debug message")





















